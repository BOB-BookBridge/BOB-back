package com.bob.core.adapter.common.symbol;

import lombok.RequiredArgsConstructor;

@RequiredArgsConstructor
public enum ResponseSymbol {
    OK, FAILED, CREATED, UPDATED, DELETED, SENT, VERIFIED;
}

